<h1>Your Bucket Lists</h1>

<%= @bucket_lists.each do |f| %>
  <%= f.name %>
  <%= link_to 'View', user_bucket_list_path(current_user, f.id) %>
  <%= link_to "Delete", user_bucket_list_path(current_user, f.id), :method => :delete %>
  <br>
<% end %>
